services:
  postgres:
    container_name: navi_postgres
    hostname: navi_postgres
    image: pgvector/pgvector:pg16
    environment:
      - POSTGRES_USER=root
      - POSTGRES_PASSWORD=root
    volumes:
      - ../../var/postgres:/var/lib/postgresql/data
